@model HelpdeskApp.Models.Ticket

<h2>Ticket Details</h2>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Ticket #@Model.Id</h4>
        <p class="card-text"><strong>Title:</strong> @Model.Title</p>
        <p class="card-text"><strong>Description:</strong> @Model.Description</p>
        <p class="card-text"><strong>Created by:</strong> @Model.Username</p>
        <a href="@Url.Action("Index", "Ticket")" class="btn btn-secondary">Back to List</a>
    </div>
</div>
